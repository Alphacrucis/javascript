function addDefaultButtons(container, creating_group, ok_action, cancel_action) {    var group;    if (creating_group)         group = container.add("group", undefined, "group_bt");    else        group = container;            group.add("button", undefined, "Aceptar", { name:"bt_ok" } );    group.add("button", undefined, "Cancelar", { name:"bt_cancel" } );        group.bt_ok.onClick = ok_action;    group.bt_cancel.onClick = (cancel_action) ? cancel_action : function() { this.window.close(); }        group.window.cancelElement = group.bt_cancel;    group.window.defaultElement = group.bt_ok;            return group;}