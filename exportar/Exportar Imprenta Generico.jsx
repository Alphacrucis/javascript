#target indesignvar dst = Folder.selectDialog ('Destino');if (dst){    var range = prompt('Páginas', app.activeDocument.pages[0].name + '-' + app.activeDocument.pages[-1].name );    if (range)    {        var rIni = parseInt(range.split('-')[0]);        var rFin = parseInt(range.split('-')[1]);        var r = rFin - rIni + 1;        for (var i=0; i<r; ++i)        {            app.pdfExportPreferences.pageRange = ''+ (rIni + i);            app.pdfExportPreferences.viewPDF = false;            app.activeDocument.exportFile(                ExportFormat.pdfType,                new File(dst+'/pdf_'+(rIni+i)+'.pdf'),                false,                app.pdfExportPresets.itemByName('Imprenta_Generico')            );        }    }}