/*        if (cs1.length < cs2) swap(cs1, cs2);                var i=0, e=cs1.length;        while (i<e && cs1[i] == cs2[i]) ++i;                if (i == e) return "";                var j=i+1;        while (j<e && cs1[j] == cs2[j]) ++i;                if (i<e) return cs1.slice(i).split(' ')[0];        if (i<cs2.length) return cs2.slice(i).split(' ')[0];        return "";    */function diff(str1, str2){    var cs1 = str1.split(/\s+/);    var cs2 = str2.split(/\s+/);        var i=0; var ei = cs1.length;    var j=0; var ej = cs2.length;    while (i<ei && j<ej)    {        while (cs1[i] == cs2[j])         {             ++i; ++j;         }            return cs1[i] + ' <--> ' + cs2[j];    }        return (ei<ej) ? cs2.slice(j) : cs1.slice(i);}$.writeln(diff("En un lugar de la macha de cuyo", "En otro  lugar de la mancha para cuyo"));