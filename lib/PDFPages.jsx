#strict on/*  Get PDF page count        Devuelve el número de páginas del documento pdf    */function pageCountPDF(pdfFileName)/*  Cuenta las veces que aparece la expresión '/Type/Page' en el documento,    ya que se supone que aparece una vez por cada página    */   {    var pdfFile = File(pdfFileName);     pdfFile.open("r");        var rg = /\/Type\/Page\b/;       var pageCount = 0;    var line;        try {        while (!pdfFile.eof) {            line = pdfFile.readln();                   if (rg.test(line))                ++pageCount;        }    } finally {         pdfFile.close();     }    return pageCount;}