var data=[];$.writeln("Procesando peticion");var conn = new Socket();if (conn.open("ibiza.ikea.es:80")){    $.writeln("Socket Open");    var result = conn.write('GET /esi/es/catalog/products/69843871?type=xml&dataset=prices HTTP/1.0\n\n');    var data = [conn.readln()];    while (!conn.eof){        data.push(conn.readln());    }    conn.close();    $.writeln(data);}