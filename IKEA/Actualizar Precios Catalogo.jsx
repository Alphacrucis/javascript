#target "indesign"#targetengine "medianis"#strict on// http://extendables.org/#include "lib/lib_codigos.jsx"function crearColor(doc, nombre, rgb){    var color = doc.colors.itemByName(nombre);    if (color != null) return color    return doc.colors.add( { name:nombre, colorValue:rgb } );}function UITable(values) {    var rce = """palette {                     orientation:'row', alignChildren:'fill',                     text: 'TABLA PRECIOS',                     preferredSize:[450, 520],                    mainPanel: Panel {                        alginment:'fill',                        alignChildren:'fill',                        table : ListBox {                             preferredSize:[420, 475],                             properties:{ numberOfColumns:3, showHeaders:true, columnTitles:["Código", "Precio", "Precio Ref."],                            columnWidths:[160, 130, 130], },                         },                        g_input : Group {                            input : EditText { visible : true, preferredSize:[330, 30], },                            b_ok : Button { text:'Ok', preferredSize:[30,30], },                            b_cn : Button { text:'X', preferredSize:[30,30], },                        },                    },                  }""";             var wnd = new Window(rce);    wnd.selected = null;        values.forEach( function(v) {        var row = wnd.mainPanel.table.add('item', v[0]);        row.subItems[0].text = v[1]        row.subItems[1].text = v[2];    });    wnd.mainPanel.table.onClick = function() {        if (this.selection) {            wnd.selected = this.selection;            wnd.mainPanel.g_input.input.text = this.selection.subItems[0].text;        }    };    wnd.mainPanel.g_input.b_cn.onClick = function () {         wnd.selected && wnd.mainPanel.g_input.input.text = wnd.selected.subItems[0].text;     };    wnd.mainPanel.g_input.b_ok.onClick = function () {         wnd.selected && wnd.selected.subItems[0].text = wnd.mainPanel.g_input.input.text;     };    wnd.show();}function extractCodePricePairs(doc, pg){    var pairs = [];        var tf = textFramesGrep(doc, rePrice, pg);        pg && pg.setMax(tf.length);    pg && pg.show("Buscando códigos");        tf.forEach(function(textFrame) {        pairs = pairs.concat(getIKEACodePricePairs(textFrame));        pg && pg.step("Buscando códigos: " + pairs.length.toString());    });    return pairs.sort(function(a, b) {         if (a[0] < b[0]) return -1;        else if (a[0] > b[0]) return 1;        else return 0;     }); // Use custom comparator}function mkUnique(arr, item_i) {    var new_arr = [];        var i = item_i ? item_i : 0;        var set = {};    arr.forEach( function(row) {         if (set[row[i]] == undefined) {            new_arr.push(row);                    set[row[i]] = true;        }    } );    set = null;    return new_arr;}function mkObject(arr, item_i) {    var Obj = {};    var i = item_i ? item_i : 0;        arr.forEach( function(row) { Obj[ row[i] ] = row; } );    return Obj;}/* Se genera un fichero csv con la siguiente extructura:        código; precio en indesign; precio original; precio actualizar;*         donde el '*' es una marca que aparece cuando precio en indesign no         coincide con precio original */function saveTableToFile(file, table, unique, show) {    var dst = file ? File(file) : File.saveDialog("Indicar fichero de destino");    var f_dst = File(dst);        f_dst.open("w");    f_dst.writeln("Código;Precio Indesign;Precio Original;Precio Actualizar");    var tableF = unique ? mkUnique(table) : table;    tableF.forEach( function(row) {         f_dst.writeln(row.join(";")); }     );    tableF = undefined;    f_dst.close();        if (show)        f_dst.execute();}    function loadDataFromFile(fileName, sepBy) {    var sep = sepBy ? sepBy : ";";        var file = File(fileName);    file.open();        var data = [];    while (! file.eof ) {        var line = file.readln().split(sep);        var item = [];        for (var i=0; i<line.length; ++i)             item.push(line[i]);        data.push( item );    }        file.close();        return data;}function filterCode(codePricePairs, pair, i) {    var r = codePricePairs.filter(function(item) { return item[0] === pair[0]; })    if (r && r.length > 0)         return r[0][i];    else         return "";}function updatePrices(doc, table, matchingPrices, pg) {    var colorCAMBIO = crearColor(doc, "CAMBIADO", [75, 0, 100, 0]);        pg && pg.show("Actualizando precios");    pg && pg.setMax(table.length);    table.forEach( function(row) {        var code = row[0];                pg && pg.step("Actualizando precios: " + code);        if (matchingPrices[code] != undefined) {            var match = matchingPrices[code];                        row[3].fillColor = colorCAMBIO;            row[3].contents = match[3];        }    });}function main() {    var pg = ProgressBar(0, "Generando listado de códigos y precio del documento")    pg.show("Cargando lista de precios originales");    /* El fichero de precios originales ha de tener la siguiente estructura:            "código;precio original;precio actualizado"    */    var referenceTable = loadDataFromFile(File.openDialog ("Fichero con precios originales"));        if (referenceTable) {        // relación de códigos y precios en el documento actual en formato [codigo, precio, Object Word (codigo), Object Word precio]        var table = extractCodePricePairs(app.activeDocument, pg);                // Crea una tabla extendida con formato: [código, precio en página, precio de referencia, precio nuevo]        var xTable = table.map( function(pair) {            return [ pair[0], pair[1], filterCode(referenceTable, pair, 1), filterCode(referenceTable, pair, 2) ];        });                // saveTableToFile(null, xTable, true);                // Crea una Objeto con aquellos productos que coincide el precio en página con el precio de referencia        var matchingPrices = mkObject(xTable.filter( function(row) { return row[1] === row[2]; } ));                // Crea una tabla con aquellos productos EN LOS QUE NO COINCIDE el precio en página con el precio de referencia        var wrongPrices = xTable.filter( function(row) { return row[1] !== row[2]; } );//        saveTableToFile('~/CodigoPrecios_error.csv', wrongPrices, true, false);        // HACE LA ACTUALIZACIÓN DE PRECIOS        updatePrices(app.activeDocument, table, matchingPrices, pg);        if (wrongPrices && wrongPrices.length > 0)            UITable(mkUnique(wrongPrices));                referenceTable = null;        table = null;        xTable = null;    }    pg.close();}app.doScript(main,    ScriptLanguage.JAVASCRIPT   , null  , UndoModes.ENTIRE_SCRIPT  , 'Actualizar Precios');