#target indesign/*  prepareToWeb    ------------        Extrae información de tipografías y colores de la página `page`     Extrae las fotos a la carpeta `folder` */function prepareToWeb(page, folder){    var F = Folder(folder);    if (folder)    {        if (!F.exists) if (!F.create()) throw "ERROR: No se puede crear la carpeta destino"    }            }/*  verifyForWeb    ------------        Comprueba que el documento actual sea válido para exportar a web    */function verifyForWeb(){}verifyForWeb();prepareToWeb(app.activeWindow.activePage, '~/tmp/pe/pi');